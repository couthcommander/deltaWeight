Generate test data set with R:

`R CMD BATCH makeWeight.R`

Run python script:

`python checkWeight.py weightFile.csv weightFile2.csv -lb 100 -ub 270`

`python deltaWeight.py weightFile2.csv weightFile3.csv`
